% antiblocker of
% \abl{K}
\DeclareMathOperator{\abl}{abl}

% absolute value
% \abs{x}
% \abs[\big]{\sum_{i \in I} x_i}
% \abs*{\sum_{i \in I} x_i}
% taken from mathtools documentation
\DeclarePairedDelimiter\abs{\lvert}{\rvert}

% adjugate matrix
% \adj(A)
\DeclareMathOperator{\adj}{adj}

% affine hull
% \aff(X)
\DeclareMathOperator{\aff}{aff}

% function composition
% f \after g
\newcommand*{\after}{\mathbin{\circ}}

% argmax
% \argmax_{x \in X} f(x)
\DeclareMathOperator*{\argmax}{arg\,max}

% argmin
% \argmin_{x \in X} f(x)
\DeclareMathOperator*{\argmin}{arg\,min}

% automorphism group
% \Aut(K)
\DeclareMathOperator{\Aut}{Aut}

% unit ball
% \Ball
\newcommand*{\Ball}{\mathbb{B}}

% boundary
% \bd(S)
\DeclareMathOperator{\bd}{bd}

% boolean quadric body
% \BQbody{V}
\newcommand*{\BQbody}[1]{\operatorname{BQ}_{#1}}

% capacity of a polynomial
% \capacity(p)
\DeclareMathOperator{\capacity}{cap}

% cardinality
% \card{S}
% \card[\Bigg]{\bigcup_{i \in I} A_i}
% \card*{\bigcup_{i \in I} A_i} uses \left and \right
\DeclarePairedDelimiter\card{\lvert}{\rvert}

% ceiling
% \ceiling{x}
% \ceiling[\Bigg]{\sum_{i \in I} x_i}
% \ceiling*{\sum_{i \in I} x_i} uses \left and \right
\DeclarePairedDelimiter\ceiling{\lceil}{\rceil}

% closure
% \cl(X)
\DeclareMathOperator{\cl}{cl}

% set of complex numbers
% \complex
\newcommand*{\Complex}{\mathbb{C}}

% function composition
% f \compose g
\newcommand*{\compose}{\after}

% (column) vector
% \colvec{a \\ b \\ c \\ ...}
\newcommand{\colvec}[1]{\begin{bmatrix} #1 \end{bmatrix}}

% cone generated by
% \cone(X)
\DeclareMathOperator{\cone}{cone}

% complexity of a polyhedron
% \cmp(P)
\newcommand*{\cmp}{\textrm{c}}

% convex hull
% \conv(X)
\DeclareMathOperator{\conv}{conv}

% coproduct
% G \coproduct H
\newcommand*{\coproduct}{\mathbin{\overline{*}}}

% corank
% \corank(M)
\DeclareMathOperator{\corank}{corank}

% cosum
% G \cosum H
\newcommand{\cosum}{\mathbin{\overline{+}}}

% curly braces
% \curly{x}
% \curly[\Bigg]{\sum_{i \in I} x_i}
% \curly*{\sum_{i \in I} x_i} uses \left and \right
\DeclarePairedDelimiter\curly{\{}{\}}

% d for (univariate) differential
% \frac{\d}{\d x}
\renewcommand*{\d}{\mathrm{d}}

% del
% \frac{\del f}{\del x}
\newcommand*{\del}{\partial}

% delta in
% \deltain(S)
% \deltain[D](S)
\newcommand*{\deltain}[1][]{\delta_{#1}^{\scriptscriptstyle \textrm{in}}}

% delta out
% \deltaout(S)
% \deltaout[D](S)
\newcommand*{\deltaout}[1][]{\delta_{#1}^{\scriptscriptstyle \textrm{out}}}

% diagonal matrix from vector
% \Diag(X)
\DeclareMathOperator{\Diag}{Diag}

% extract diagonal from matrix
% \diag(X)
\DeclareMathOperator{\diag}{diag}

% diameter
% \diam(X)
\DeclareMathOperator{\diam}{diam}

% in degree
% \din(S)
% \din[D](S)
\newcommand*{\din}[1][]{d_{#1}^{\scriptscriptstyle \textrm{in}}}

% distance
% \dist_G(r,s)
\DeclareMathOperator{\dist}{dist}

% effective domain
% \dom(f)
\DeclareMathOperator{\dom}{dom}

% dominant
% \dominant{P}
\newcommand*{\dominant}[1]{{#1}^{\uparrow}}

% out degree
% \dout(S)
% \dout[D](S)
\newcommand*{\dout}[1][]{d_{#1}^{\scriptscriptstyle \textrm{out}}}

% drop
% V \drop S
\newcommand*{\drop}{\setminus}

% elliptope
% \Elliptope{V}
\newcommand*{\Elliptope}[1]{\Escr_{#1}}

% the empty set
% \emptyset
\let\oldemptyset\emptyset
\renewcommand*{\emptyset}{\varnothing}

% epigraph of a function
% \epi(f)
\DeclareMathOperator{\epi}{epi}

% epsilon
% \eps
\newcommand*{\eps}{\varepsilon}

% underbracket of an expression as something equal
% \equalto{\iprod{x}{x}}{0}
\newcommand{\equalto}[2]{\underbracket[.5pt]{#1}_{\scriptstyle\overset{\mkern4mu\rotatebox{90}{$\,=$}}{#2}}}

% evaluated at
% \evalat{\frac{\d}{\d x}e^{\alpha x}}{x=0}
\DeclarePairedDelimiterXPP{\evalat}[2]{}{.}\rvert{_{#2}}{\mathopen{}#1}

% evaluated at (with square bracket as left delimiter)
% \evalatsq{\frac{\d}{\d x}e^{\alpha x}}{x=0}
\DeclarePairedDelimiterXPP{\evalatsq}[2]{}{[}\rvert{_{#2}}{\mathopen{}#1} %]

% extreme rays of a cone
% \Ext(K)
\DeclareMathOperator{\Ext}{Ext}

% set of extreme points
% \ext(P)
\DeclareMathOperator{\ext}{ext}

% set of faces of a convex set
% \Faces(X)
\DeclareMathOperator{\Faces}{Faces}

% face or equal to
% F \faceeq X
\newcommand*{\faceeq}{\mathrel{\unlhd}}

% proper face of
% F \faceneq X
\newcommand*{\faceneq}{\mathrel{\lhd}}

% function definition
% f \ffrom V \fto \Reals
\newcommand*{\ffrom}{\colon}

% floor
% \floor{x}
% \floor[\Bigg]{\sum_{i \in I} x_i}
% \floor*{\sum_{i \in I} x_i} uses \left and \right
\DeclarePairedDelimiter\floor{\lfloor}{\rfloor}

% weak fractional stable set polytope FRAC
% \FRAC
\DeclareMathOperator{\FRAC}{FRAC}

% function definition
% f \ffrom V \fto \Reals
\newcommand*{\fto}{\to}

% core of a graph
% G^{\core}
\newcommand{\core}{\bullet}

% general linear group
% \GL(V)
\DeclareMathOperator{\GL}{GL}

% half-closed interval (open left, closed right)
% \halfclosed{x,y}
\DeclarePairedDelimiter\halfclosed{(}{]}

% half-open interval (closed left, open right)
% \halfopen{x,y}
\DeclarePairedDelimiter\halfopen{[}{)}

% set of Hermitian matrices
% \Her{V}
\newcommand{\Her}[1]{\mathbb{H}^{#1}}

% set of Hermitian matrices that are pd
% \HPd{V}
\newcommand{\Hpd}[1]{\mathbb{H}_{++}^{#1}}

% Hadamard product
% A \hprod B
\newcommand*{\hprod}{\mathbin{\odot}}

% set of Hermitian matrices that are psd
% \HPsd{V}
\newcommand{\Hpsd}[1]{\mathbb{H}_+^{#1}}

% hypograph of a function
% \hypo(f)
\DeclareMathOperator{\hypo}{hypo}

% ignore
% \ignore{this will not appear}
\newcommand{\ignore}[1]{}

% imaginary unit (to distinguish it from dummy index i)
% a+\im b
\newcommand*{\im}{\mathrm{i}}

% image/range
% \Image(A)
\DeclareMathOperator{\Image}{Im}

% incidence vector
% \incidv{F}
\newcommand*{\incidv}[1]{\mathbbm{1}_{#1}}

% indicator function
% \indicf{S}{x}
\DeclarePairedDelimiterXPP{\indicf}[2]{\delta}{(}{)}{}{#2\,\delimsize\vert\,\mathopen{}#1}

% infimal convolution
% f \infconv g
\newcommand*{\infconv}{\mathbin{\square}}

% infinity norm
% \infnorm{x}
\newcommand{\infnorm}[1]{\norm{#1}_{\infty}}

% set of integers
% \Integers
\newcommand*{\Integers}{\mathbb{Z}}

%  interior
\DeclareMathOperator{\interior}{int}

% inner product, <.,.> notation
% \iprod{I}{X}
\DeclarePairedDelimiterXPP{\iprod}[2]{}{\langle}{\rangle}{}{#1,\mathopen{}#2}

% symmetric inner product
% \iprodsym{x}
\DeclarePairedDelimiterXPP{\iprodsym}[1]{}{\langle}{\rangle}{}{#1,\mathopen{}#1}

% inner product, .^T. notation
% \iprodt{x}{y}
\newcommand{\iprodt}[2]{#1^{\transp}#2}

% symmetric inner product, .^T. notation
% \iprodtsym{x}
\newcommand{\iprodtsym}[1]{#1^{\transp}#1}

% Iverson notation
% \Iverson{x > 0}
% \Iverson[\Big]{\sum_{i=1}^n x_i > 0}
% \Iverson*{\sum_{i=1}^n x_i > 0} uses \left and \right
\DeclarePairedDelimiter\Iverson{[}{]}

% decreasing vector of eigenvalues
% \lambdadown(X)
% \lambdadown[1](X)
\newcommand*{\lambdadown}[1][]{\lambda_{#1}^{\downarrow}}

% maximum eigenvalue
% \lambdamax(X)
\newcommand*{\lambdamax}{\lambda_{\max}}

% minimum eigenvalue
% \lambdamin(X)
\newcommand*{\lambdamin}{\lambda_{\min}}

% increasing vector of eigenvalues
% \lambdaup(X)
% \lambdaup[1](X)
\newcommand*{\lambdaup}[1][]{\lambda_{#1}^{\uparrow}}

% lifted theta body
% \liftedTH(G)
\newcommand*{\liftedTH}{\widehat{\THbody}}

% linear hull
% \lin(X)
\DeclareMathOperator{\lin}{lin}

% linear span (unfortunately, \span is already defined) == linear hull
% \linspan\set{\ones}
\DeclareMathOperator{\linspan}{span}

% max-k-colorable problem
\newcommand*{\maxkcol}[1][]{\mbox{\textsc{Max-}\(\ifthenelse{\equal{#1}{}}{k}{#1}\)\textsc{-Col}}}

% Minkowski functional
% \Minkf{S}{x}
\DeclarePairedDelimiterXPP{\Minkf}[2]{\gamma}{(}{)}{}{#2\,\delimsize\vert\,\mathopen{}#1}

% Moore-Penrose pseudoinverse
% X^{\MPinv}
\newcommand*{\MPinv}{\dagger}

% natural numbers
\newcommand{\Naturals}{\mathbb{N}}

% avoids unpleasant line breaks
% $k$\nbd-connected
\newcommand*{\nbd}{\nobreakdash}

% norm
% \norm{x}
% \norm[\Big]{\sum_{i \in I} x_i}
% \norm*{\sum_{i \in I} x_i} uses \left and \right
\DeclarePairedDelimiter\norm{\lVert}{\rVert}

% nullspace
% \Null(A)
\DeclareMathOperator{\Null}{Null}

% number of odd components in a graph
% \odd(G)
\DeclareMathOperator{\odd}{odd}

% Requires: \usepackage{bbm}
% vector of all-ones
\newcommand*{\ones}{\mathbbm{1}}

% outer product, xy^T
% \oprod{x}{y}
\newcommand{\oprod}[2]{#1^{}#2^{\transp}}

% symmetric outer product
% \oprodsym{x}
%
% The ^{} is added after #1 to ensure that indices in \oprod have the
% same vertical alignment; note that #1 is already expected not to
% have any superscripts anyway
\newcommand{\oprodsym}[1]{\oprod{#1}{#1}}

% optimal value
% \OPT
\DeclareMathOperator{\OPT}{OPT}

% parentheses
% \paren{x}
% \paren[\Bigg]{\sum_{i \in I} x_i}
% \paren*{\sum_{i \in I} x_i} uses \left and \right
\DeclarePairedDelimiter\paren{(}{)}

% r-arborescence polytope
% \Parb
\newcommand*{\Parb}[1][]{P_{r\textrm{-arb}}({#1})}

% cut polytope
% \Pcut
\newcommand{\Pcut}{P_{\textrm{cut}}}

% cone of positive definite matrices
% \Pd{n}
\newcommand*{\Pd}[1]{\mathbb{S}_{++}^{#1}}

% permanent
% \per(A)
\DeclareMathOperator{\per}{per}

% matching polytope
% \Pm
\newcommand{\Pm}{P_{\Mcal}}

% set of perfect matchings of G
% \Pmatch(G)
\newcommand{\Pmatch}{\Pcal\Mcal}

% p-norm
% \pnorm{x}{\infty}
% \pnorm[\Big]{\sum_{i \in I} x_i}{\infty}
% \pnorm*{\sum_{i \in I} x_i}{\infty} uses \left and \right
\DeclarePairedDelimiterXPP{\pnorm}[2]{}\lVert\rVert{_{#2}}{#1}

% subsets of odd cardinality
% \Podd(S)
\newcommand{\Podd}{\Pcal_{\textrm{odd}}}

% polar
% \beta^{\polar}
\newcommand*{\polar}{\circ}

% power set
% \Powerset{V}
\newcommand*{\Powerset}[1]{\mathcal{P}(#1)}

% perfect matching polytope
% \Ppm
\newcommand{\Ppm}{P_{\Pcal\Mcal}}

% cone partial order
\let\oldpreceq\preceq
\renewcommand*{\preceq}{\preccurlyeq}

% orthogonal projection onto L
% \Proj_L
\DeclareMathOperator{\Proj}{Proj}

% cone of positive semidefinite matrices
% \Psd{n}
\newcommand*{\Psd}[1]{\mathbb{S}_+^{#1}}

% cone of (lifted) positive semidefinite matrices
% \Psdhat{n}
\newcommand*{\Psdhat}[1]{\widehat{\mathbb{S}}_+^{#1}}

% quadratic form, x^T Q x
% \qform{Q}{x}
\newcommand{\qform}[2]{#2^{\transp}#1#2}

% fractional stable set polytope
% \QSTAB(G)
\DeclareMathOperator{\QSTAB}{QSTAB}

% rank
% \rank(X)
\DeclareMathOperator{\rank}{rank}

% nonnegative rank
% \rankp(A)
\newcommand*{\rankp}{\textrm{rank}^+}

% set of rationals
% \Rationals
\newcommand*{\Rationals}{\mathbb{Q}}

% reaches
% r \reaches_D s
\newcommand*{\reaches}{\rightsquigarrow}

% set of reals
% \Reals
\newcommand*{\Reals}{\mathbb{R}}

% recession cone of a set
% \reccone{G}
\newcommand*{\reccone}[1]{0^{+}#1}

% related by (equation number on top of a relation sign)
% \relby{\succeq}{\cref{eq:1}}
\newcommand{\relby}[2]{\overset{\mathclap{#2}}{#1}}

% function restriction
% f\restrict{S}
\newcommand*{\restrict}[1]{\mathord{\restriction}_{#1}}

% relative interior
\DeclareMathOperator{\ri}{ri}

% sequence
% \seq{v_1,\dotsc,v_n}
\DeclarePairedDelimiter{\seq}{\langle}{\rangle}

% set curly braces
% \set{1,2,\dotsc,n}
% \set[\Bigg]{\bigcup_{i \in I} A_i}
\DeclarePairedDelimiter\set{\{}{\}}

% set curly braces, with condition separated by colon
% \setst{x \in \Reals^n}{Ax = b}
% \setst[\Bigg]{x \in \Reals^n}{Ax = b,\, x = \sum_{i \in I} x_i}
% \setst*{x \in \Reals^n}{Ax = b,\, x = \sum_{i \in I} x_i} uses \left and \right
\DeclarePairedDelimiterXPP\setst[2]{}\{\}{}{\,#1\,\colon\mathopen{}#2}

% set curly braces, with condition separated by colon
% \setbar{x \in \Reals^n}{Ax = b}
% \setbar[\Bigg]{x \in \Reals^n}{Ax = b,\, x = \sum_{i \in I} x_i}
% \setbar*{x \in \Reals^n}{Ax = b,\, x = \sum_{i \in I} x_i} uses \left and \right
\DeclarePairedDelimiterXPP\setbar[2]{}\{\}{}{\,#1\nonscript\:\delimsize\vert\nonscript\:\mathopen{}#2}

% sign
% \sign(x)
\DeclareMathOperator{\sign}{sign}

% sign
% \sgn(f)
\DeclareMathOperator{\sgn}{sgn}

% slack matrix of a polyhedron
% \Slack(P)
\newcommand*{\Slack}{\textrm{Slack}}

% square brackets
% \sqbrac{x}
% \sqbrac[\Bigg]{\sum_{i \in I} x_i}
\DeclarePairedDelimiter\sqbrac{[}{]}

% such that
\newcommand*{\st}{\colon}

% stable set polytope
% \STAB(G)
\DeclareMathOperator{\STAB}{STAB}

% cone partial order
\let\oldsucceq\succeq
\renewcommand*{\succeq}{\succcurlyeq}

% support
% \supp(x)
\DeclareMathOperator{\supp}{supp}

% support function
% \suppf{S}{x}
\DeclarePairedDelimiterXPP{\suppf}[2]{\delta^*}{(}{)}{}{#2\,\delimsize\vert\,\mathopen{}#1}

% space of symmetric matrices
% \Sym{n}
\newcommand*{\Sym}[1]{\mathbb{S}^{#1}}

% symmetric difference
% \SymDiff()
\newcommand{\SymDiff}{\mathbin{\Delta}}

% symmetric group
% \SymGroup{V}
\newcommand*{\SymGroup}[1]{\operatorname{Sym}_{#1}}

% space of (lifted) symmetric matrices
% \Symhat{n}
\newcommand*{\Symhat}[1]{\widehat{\mathbb{S}}^{#1}}

% nonnegative symmetric matrices
% \Symnonneg{n}
\newcommand*{\Symnonneg}[1]{\mathbb{S}_{\scriptscriptstyle\geq 0}^{#1}}

% theta body TH
% \THbody
\DeclareMathOperator{\THbody}{TH}

% replace theta and vartheta
\let\oldtheta\theta
\let\theta\vartheta
\let\vartheta\oldtheta

% trace
% \trace(X)
\DeclareMathOperator{\trace}{Tr}

% transpose
% X^{\transp}
\newcommand*{\transp}{\mathsf{T}}

% value of a flow
% \val{x}
\newcommand*{\val}[1]{\textrm{val}({#1})}

% replace vec operator and original \vec command (arrow)
\DeclareMathOperator{\myvec}{vec}
\let\arrowvec\vec
\let\vec\myvec

% volume
% \vol(S)
\DeclareMathOperator{\vol}{vol}

% extension complexity of a polyhedron
% \xc(P)
\newcommand*{\xc}{\textrm{xc}}

% zero lift of a set
% \Sym{\zlift{V}}
\newcommand*{\zlift}[1]{\set{0} \cup #1}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: "main"
%%% coding: utf-8
%%% sentence-end-double-space: t
%%% End: